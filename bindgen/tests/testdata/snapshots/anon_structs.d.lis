// Generated by Lisette bindgen
// Source: ./testdata/fixtures/anon_structs (local)
// Go: 0.0.0
// Lisette: 0.0.0

import "go:io"

/// Synthesized from a Go anonymous struct type.
#[go(anon_struct)]
pub struct AnonA {
  pub A: int,
}

/// Synthesized from a Go anonymous struct type.
#[go(anon_struct)]
pub struct AnonB {
  pub B: int,
}

/// Synthesized from a Go anonymous struct type.
#[go(anon_struct)]
pub struct AnonDeep {
  pub Deep: int,
}

/// Synthesized from a Go anonymous struct type.
#[go(anon_struct)]
pub struct AnonHitsMisses {
  pub Hits: int64,
  pub Misses: int64,
}

/// Synthesized from a Go anonymous struct type.
#[go(anon_struct)]
pub struct AnonInner {
  pub Inner: AnonDeep,
}

/// Synthesized from a Go anonymous struct type.
#[go(anon_struct)]
pub struct AnonLabel {
  pub Label: string,
}

/// Synthesized from a Go anonymous struct type.
#[go(anon_struct)]
pub struct AnonW {
  pub W: Option<io.Writer>,
}

/// Synthesized from a Go anonymous struct type.
#[go(anon_struct)]
pub struct AnonX {
  pub X: int,
}

/// OptionReturn returns an anonymous struct with a comma-ok bool.
pub fn OptionReturn() -> Option<AnonLabel>

/// PlainReturn returns an anonymous struct directly.
pub fn PlainReturn() -> AnonX

/// ResultReturn returns an anonymous struct alongside an error.
pub fn ResultReturn() -> Result<AnonX, error>

/// Parameter position; constructible from Lisette because every field is exported.
pub fn TakesAnon(p: AnonX)

/// TupleReturn returns two distinct anonymous structs.
pub fn TupleReturn() -> (AnonA, AnonB)

/// Collection, pointer, and channel element positions. The array element still
/// gates, because fixed-size arrays are unrepresentable, so only that field is
/// dropped.
pub struct Carriers {
  pub Slice: Slice<AnonX>,
  pub MapKey: Map<AnonX, int>,
  pub MapVal: Map<string, AnonX>,
  pub Ptr: Option<Ref<AnonX>>,
  pub Chan: Channel<AnonX>,
  // SKIPPED field "Array": array-currently-not-representable — fixed-size array cannot currently be represented in Lisette
}

/// Struct fields, including a nested anonymous struct and one carrying an
/// external (io) type.
pub struct Holder {
  pub Direct: AnonX,
  pub Nested: AnonInner,
  pub Writer: AnonW,
}

/// OnlyInArray's anonymous element appears nowhere else. The array gates, so the
/// field drops; the element synthesis must not leak a type into the output.
pub struct OnlyInArray {
  // SKIPPED field "Items": array-currently-not-representable — fixed-size array cannot currently be represented in Lisette
  pub Name: string,
}

/// Variable position.
pub var Anon: AnonHitsMisses
