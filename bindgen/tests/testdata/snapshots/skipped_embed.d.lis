// Generated by Lisette bindgen
// Source: ./testdata/fixtures/skipped_embed (local)
// Go: 0.0.0
// Lisette: 0.0.0

pub struct Box<T> {
  pub V: T,
}

/// Embeds Box[[2]int]: the array type argument is unrepresentable, so bindgen
/// skips the field; Host.M must stay flattened, not dropped with the skipped embed.
#[go(hidden_embed)]
pub struct Host {
  // SKIPPED field "Box": array-currently-not-representable — fixed-size array cannot currently be represented in Lisette
  pub Y: int,
}

/// Exported embed of an internal-package type: bindgen skips the field, so Widget
/// looks flat but must stay unembeddable (marked) with hidden.Engine.Run kept.
#[go(hidden_embed)]
pub struct Widget {
  // SKIPPED field "Engine": internal-package-ref — references type from internal package "github.com/ivov/lisette/bindgen/tests/testdata/fixtures/skipped_embed/internal/hidden"
  pub X: int,
}

impl<T> Box<T> {
  fn M(self) -> int
}

impl Host {
  fn M(self) -> int
}

impl Widget {
  fn Run(self) -> int
}
